name: Candidate 129 adjacent transport

on:
  pull_request:
    branches:
      - research/general-reduction-n8-d3
    paths:
      - "research/candidate129/**"
      - ".github/workflows/candidate129-adjacent.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: candidate129-adjacent-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  adjacent:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Run exact adjacent-amplitude transport closure
        run: |
          set -euo pipefail
          /usr/bin/time -v python research/candidate129/probe_candidate129_adjacent.py \
            research/candidate129/candidate129_support.txt \
            research/candidate129/candidate129_adjacent.json \
            2>&1 | tee research/candidate129/candidate129_adjacent.log
      - name: Upload adjacent-transport outputs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: candidate129-adjacent-${{ github.run_id }}
          path: |
            research/candidate129/candidate129_support.txt
            research/candidate129/candidate129_adjacent.json
            research/candidate129/candidate129_adjacent.log
          if-no-files-found: warn
          retention-days: 30
